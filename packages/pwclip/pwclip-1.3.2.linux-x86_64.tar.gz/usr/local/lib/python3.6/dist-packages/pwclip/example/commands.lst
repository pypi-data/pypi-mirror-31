# one-shot convert key/cert in openssl x509 format, read passwords from passwords.yaml and securely list them
pwcli -Y passwords.yaml --cert ssl.crt --key ssl.key --ca-cert ca.crt -l

# to securely list passwords of all (system) users in keystore
pwcli -Al

# to run in gui mode just use
pwclip
# or
ykclip
